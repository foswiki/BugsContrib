package Foswiki::Contrib::BugsContrib;
use strict;

# This should always be $Rev: 2085 $ so that Foswiki can determine the
# checked-in status of the plugin. It is used by the build automation tools, so
# you should leave it alone.
our $VERSION = '$Rev: 2085 $';

# This is a free-form string you can use to "name" your own plugin version.
# It is *not* used by the build automation tools, but is reported as part
# of the version number in PLUGINDESCRIPTIONS.
our $RELEASE = '4 Oct 2010';

our $SHORTDESCRIPTION =
  'Item Tracking system implemented as a Foswiki Application';

1;
